using Cysharp.Threading.Tasks;
using UnityEngine;

public class TutorialManager : MonoBehaviour
{
   /*  public static TutorialManager Instance;

    [Header("튜토리얼 제어")]
    [SerializeField] private EnemyWaveSpawner waveSpawner; // 웨이브 스포너 연결
    [SerializeField] private GameObject[] tutorialEnemies; // 튜토리얼용 적들

    private void Awake() => Instance = this;

    private async void Start()
    {
        // 1. 게임 시작 시 웨이브 스포너 중단
        waveSpawner.isTutorialArea = true;

        // 2. 튜토리얼 흐름 실행
        await RunTutorialSequence();
    }

    private async UniTask RunTutorialSequence()
    {
        // 줌인 -> 설명 -> 대기 -> 다음 단계 순서로 진행
        // 예: 카메라 줌(Cinemachine 활용)
        // await ShowTutorialUI("체력바 설명");
        // await WaitForKey(KeyCode.Return);

        // 마지막에 웨이브 재개
        waveSpawner.isTutorialArea = false;
        UtillLogRemove.Log("튜토리얼 종료, 적 웨이브 시작");
    }  */
}
